{
  "_comment": "Copy to PhonemeSubstitutions.json (gitignored) to customise phonemization. 'substitutions' rewrite spellings before phonemization (regex, case-insensitive). 'phoneme_overrides' force a word's IPA verbatim (key = lowercased word text as gruut sees it, value = IPA using symbols from text_utils.py).",
  "substitutions": [
    { "pattern": "\\bDr\\.\\b", "replacement": "Doctor" }
  ],
  "phoneme_overrides": {
    "gif": "dʒˈɪf"
  }
}
